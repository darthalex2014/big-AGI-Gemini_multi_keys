import * as React from 'react';
import { Box, Typography } from '@mui/joy';

export function AppManager() {
  return (
    <Box sx={{ p: 2 }}>
      <Typography level="h2">Manager</Typography>
      <Typography level="body-sm">This is the Manager interface, work in progress</Typography>
    </Box>
  );
}
